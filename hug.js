const {
    MessageEmbed,
    Message
} = require ("discord.js");
const { description } = require("./mute");
module.exports = {
    name: 'hug',
    description: 'Sends a hug gif',
    async execute (message) {
        const hugGIFs = []
    }
}